src/cbeepsynth/TODO.txt

- figure out how to get this to compile correctly with GCC/msys2 ucrt64/windows
  (I think GCC is better at compiler optimizations and vectorization.)
  (okay, turns out it wasn't broken, i just forgot to make it statically link
   the C++ runtime. but standalone just freezes on launch. how come?)
- annoying that I have to allocate a bpbxsyn_context for each plugin instance,
  because that is where the logging function is located. but also I think it
  doesn't really make sense for individual synths/effects in the bbsyn API to
  have a logging function. maybe just don't hook up the library to the plugin's
  logging system? it's not like it logs out anything useful anyway, aside from
  allocation errors that should never happen.
- apparently, win32 standalone wrapper calls deinit before plugin_destroy.
  update clap-wrapper? maybe there's a fix there? if not, fix it yourself and
  submit a PR.
- standalone plugins for effects:
	- BeepBox Distortion (M)  (for mono)
	- BeepBox Distortion (S)  (for stereo)
	- BeepBox Bitcrusher (M)
	- BeepBox Bitcrusher (S)
	- BeepBox Chorus
	- BeepBox Echo
	- BeepBox Reverb
	- BeepBox Compressor/Limiter


	or, perhaps:
	- "BeepBox Mono Effects", which includes:
		- distortion, bitcrusher
	- "BeepBox Stereo Effects", which includes:
		- chorus, echo, reverb, limiter
		- and mono effects applied individually per channel

BUGS:
- vibrato starts from wrong beat in fl studio. Could either clap-wrapper not
  providing playback information (not likely) or my code being faulty. need to
  check with bitwig studio.
  (...does it? )

CUSTOM SAMPLES:
  There will be a list in bbsyn_context that contains the data for custom
  samples.

  ```c
  typedef struct bbsyn_custom_sample_data {
    unsigned sample_count;
	int root_key;
	bool percussion;
	float *data;
  } bbsyn_custom_sample_data_s;

  struct bbsyn_context {
    bbsyn_allocator_s alloc;
    bbsyn_log_f log_func;
    void *log_userdata;

    wavetables_s wavetables;
	unsigned custom_sample_count;
	const bbsyn_custom_sample_data_s *custom_samples;
  };
  ```

  For the chip wave synth type, chip wave indicies that reference past the
  bounds of the internal wavetable list will instead refer to the custom_samples
  list.